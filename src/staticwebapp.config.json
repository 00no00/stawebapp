{
    "networking":{
      "allowedIpRanges":["AzureFrontDoor.Backend"]
    },
    "routes": [
        {
            "route": "/",
            "allowedRoles": ["authenticated", "members"],
            "headers": {
                "Cache-Control": "no-store"
            }
        }
    ]
}
